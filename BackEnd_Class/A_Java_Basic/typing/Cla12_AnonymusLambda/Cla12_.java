package BackEnd_Class.A_Java_Basic.typing.Cla12_AnonymusLambda;

public class Cla12_ {

    public static void main(String[] args) {

/*
        <<23.03.19>>
        추상 클래스 바로 객체화 안됨 (익명클래스 연관)
       - 인터페이스 문법 메소드 : public abstract 메소드 이름, public static final 변수이름
       - 다형성
       - 내부클래스(클래스 안에 클래스)
       - 익명클래스



*/



/*


        String[] arr = {"abc", "bcddd","bccccccc"};
        String answer = "";

        int count = 0;
        for(String a : arr){
            if(a.length()>6 && a.length()<11){
                answer = a.toUpperCase(Locale.ROOT);
                count++;
                break;
            }
        }

        if(count == 0){
            answer = "없음";
        }

        System.out.println(answer);

        // boolean을 쓸때 chedcked를 true로 바로


        << stream 풀이 >>

        String answer = Arrays.stream(arr).filter(a->     ) 함수 -> arr을 매개변수로 함
        optional 값이 없을 수도 있는 상황 객체(null인지 아닌지) 없을 경우 어떻게 해라~라는 함수 사용



*/














    }

}
