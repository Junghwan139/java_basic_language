package classforBackEnd.Java_Basic.completed;

public class C_5W_Cla08to9 {

    public static void main(String[] args) {

/*

        1. 클래스 / 클래스 메소드 / 클래스 함수, 변수(static)
        ★ static이 붙은 함수, 변수는 클래스 함수, 변수라고 부름 → 객체 생성 없이, 함수, 변수 사용 가능
        ★ 같은 클래스 내에서 함수 선언 시 객체 생성이 번거로우니 static으로 사용

        2. 인스턴스 / 객체 변수, 메서드
         (1) 선언 : ① 클래스 호출( 클래스명 변수명 = new 생성자()) → ② 변수명.함수명 / 변수명
         (2) 특징 : 클래스 내용은 복사해서 개별 저장영역을 만듦 (자회사1, 자회사2..)
                   객체를 만들면 heap메모리 공간에 저장되어 휘발되지 않는 성질을 가진다.
        ★ 객체를 사용해야 하는 이유? 누적..붕어빵 틀(class) - 붕어빵(갹체) / 자회사를 가진 회사 → 자회사별 누적 손익 필요 시
         (3) 함수 : return(① 인수반환 ② 함수 종료(break)),
                   this.(매개변수와 클래스내 사용한 변수를 구분하기 위하여

        3. 접근제어자(public..) → ★★ class를 new해서 객체변수를 만들고 그 객체변수로 해당 변수에 접근할 때의 접근 권한을 관리 
         (1) 역할 : 접근권한을 설정
         (2) 종류 : public, private, default, protected
         (3) private 설정시 외부 클래스에서는 해당 변수에 접근할 수가 없으므로, 함수(getter, setter)를 사용하여
             접근 (변수에 직접 접근하는 방법은 이슈가 있어서 현업에서는 사용하지 않음 ① 변수별 get, set 함수 생성 Spring가면 잘안씀
             ② 생성자를 통한 선언 시 바로 입력)

        4. 생성자(Constructor)는 ★★메서드 인데, 클래스의 이름을 가진 메서드
         (1) 선언 : 접근제어자 class명 () {  }
         (2) 역할 : 생성자의 역할은 인스턴스 변수를 초기화하는데 있다.
         (3) 특징 : 생성자가 선언되어 있지 않으면, 매개변수가 없는 기본 생성자 생성
                   별도의 생성자를 추가하게 되면,반드시 기본 생성자를 따로 추가해줘야 한다.
                   생성자 메서드는 객체가 생성됨과 동시에 실행되는 메서드이다.
         (4) this()의 추가 역할 : this()는 같은 클래스 내에 타입에 맞는 다른 생성자를 호출
                                *super()는 타입에 맞는 부모클래스의 생성자를 호출

    <예시>
    public constructorTest(String email, String password){
        this(email, password,0);

        this.email = email;
        this.password = password;
    }



 */





    }
}
